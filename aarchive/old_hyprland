
#exec-once = swayidle -w timeout 1800 'systemctl suspend' resume 'hyprctl dispatch dpms on' before-sleep 'swaylock'
#exec-once = swayidle -w timeout 1800 'systemctl suspend' resume 'hyprctl dispatch dpms on'


## Animations
    # Defaults
    # bezier = myBezier, 0.05, 0.9, 0.1, 1.05
    # animation = windows, 1, 7, myBezier
    # animation = windowsOut, 0, 1, default, popin 80%
    # animation = border, 1, 10, default
    # animation = borderangle, 1, 8, default
    # animation = fade, 1, 7, default
    # animation = workspaces, 1, 8, default


#exec-once = dunst &
#exec-once = hyprpaper &
